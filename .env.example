# API Gateway Base URL — the single public entry point for all backend
# services. The gateway routes /api/identity/* and /api/resource/* downstream,
# so one URL is used by both the Identity and Resource clients.
# In local development with docker compose this is the gateway's host port.
VITE_API_URL=http://localhost:8080
